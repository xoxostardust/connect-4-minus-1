// Default grid piece types (red and yellow)
export const PieceType = Object.freeze({
    RED: 1,
    YELLOW: 2
});

// Default player teams (red and yellow)
export const PlayerTeam = Object.freeze({
    RED: 1,
    YELLOW: 2
});

// Default game types (one player and two players)
export const GameType = Object.freeze({
    ONE_PLAYER: 1,
    TWO_PLAYERS: 2
});

// Default game status types (to be used)
export const GameStatus = Object.freeze({
    IN_PROGRESS: 1,
    ENDED: 2
});
